# Optional override. The OAuth token is auto-loaded from
# ~/.claude/.credentials.json, so this is usually NOT needed.
# Only set TOKEN to force a specific token; it must include the "Bearer " prefix.
# This is the OAuth token (sk-ant-oat01-...), NOT a billing API key (sk-ant-api...).
# TOKEN = Bearer sk-ant-oat01--REPLACE_ME
