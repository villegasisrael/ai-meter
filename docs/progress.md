# Progress

## 2026-05-13
- Bootstrap completo del proyecto `ai-meter`.
- CLI con comandos `run`, `doctor`, `paths`, `export`.
- Config local con `network_disabled=true` obligatorio.
- SQLite + migraciones idempotentes + tabla `file_offsets`.
- Collectors locales: Codex, Claude, System.
- TUI Textual con paneles de uso IA, eventos y hardware.
- Export JSON/CSV desde SQLite.
